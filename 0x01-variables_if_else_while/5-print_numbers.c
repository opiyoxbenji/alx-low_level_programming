#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int m = 10, i = 0;

	while (i <= m)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");

	return (0);
}
