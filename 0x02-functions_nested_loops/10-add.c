#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @m: first number
 * @n: second number
 * Return: the result
 */
int add(int m, int n)
{
	int result;

	result = m + n;

	return (result);
}
