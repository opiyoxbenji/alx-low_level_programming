#include "main.h"

/**
 * _abs - computes absolute value of integer
 * Return: absolute
 * @n: integer argument
 */

int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
return (0);
}
